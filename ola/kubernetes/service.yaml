kind: Service
apiVersion: v1
metadata:
  namespace: microservices
  name: ola
  labels:
    app: ola
    demo: cicd
spec:
  selector:
    app: ola
  type: ClusterIP
  ports:
  - protocol: TCP
    name: http-3000
    port: 3000
    targetPort: 3000